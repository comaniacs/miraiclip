# Miraiclip — Plan & Feature List

An open source, framework-agnostic library for building video editors in the browser. Command-driven, AI-native, collaboration-ready. Reference point: [OpenVideo](https://docs.openvideo.dev/).

## Vision

Miraiclip is not a video editor app — it is the engine you build one with. The core is headless: it manages project state, a microsecond-precision timeline, and a full command history, with rendering delivered as a separate layer. Every mutation flows through a descriptive command, which makes the engine equally usable by a human clicking a UI, an LLM generating an edit sequence, or a sync layer replaying a collaborator's changes.

## Architectural pillars

1. **Command-Driven Architecture** — No imperative mutation of state. You dispatch descriptive commands (`ADD_CLIP`, `SPLIT_CLIP`, `MOVE_CLIP`, `SET_CLIP_PROPERTY`…). Each command is deterministic, validatable, serializable, and invertible. Commands can be batched into transactions that undo/redo as a unit.
2. **AI-Native** — Because commands are plain descriptive data with a published catalog and JSON schemas, LLMs can reason about the project state and emit valid command sequences. The command catalog doubles as the AI tool definition.
3. **Time Travel** — Built-in undo/redo backed by full history. Since every command is invertible (or paired with inverse patches), history is exact, replayable, and inspectable.
4. **Framework Agnostic** — The core has zero UI dependencies. Thin adapter packages expose idiomatic bindings for React, Vue, Svelte, or vanilla JS; the core also runs in Node for testing and server-side workflows.
5. **Collaboration-Ready** — State changes are emitted as granular patches (Immer-style JSON patches). Command + patch streams are the substrate for real-time multiplayer: broadcast commands, reconcile with patches, resolve conflicts at the command level.

## Core concepts

### Project State
The single source of truth for the composition — a Zustand (vanilla) store holding the timeline position, tracks, clips, assets, and playback state. Consumers subscribe to slices; the store is never mutated directly, only through the command dispatcher. State is fully serializable to JSON (save/load a project file).

### Commands
The only write path into the engine.
- Descriptive intents: `{ type: "clip/move", payload: { clipId, trackId, startUs } }`
- Validated against schemas before execution; invalid commands are rejected with typed errors.
- Deterministic: same state + same command = same result (a requirement for collaboration and AI replay).
- Batchable: transactions group commands into one history entry.
- Custom commands: consumers can register their own command types alongside the built-in catalog.

### Clips
The fundamental building blocks — video, audio, image, and text elements positioned on the timeline. Each clip has timeline placement (`startUs`, `durationUs`), source trimming (`trimStartUs`, `trimEndUs`), transform (position, scale, rotation, opacity), and type-specific properties. All clip operations (add, remove, move, trim, split, duplicate, property updates) go through commands.

### Tracks
Ordered containers that define layering (z-index) and group related clips. Track operations — create, remove, reorder, mute/solo, lock — are commands too. Track types (video/overlay vs audio) constrain what clips they accept.

### Timeline
Microsecond-based (1 s = 1,000,000 µs) for frame-accurate positioning at any frame rate. Utilities for µs ↔ frame ↔ timecode conversion, snapping, and overlap resolution.

### Events & Patches
A reactive event system for playback changes and state mutations. Because the engine is command-driven, changes are emitted as granular patches rather than "something changed" — subscribers (UIs, sync layers, loggers) get exactly what changed and can apply/invert it elsewhere.

### History
Undo/redo stacks built from command + inverse-patch pairs. Supports transaction grouping, history capping, and jumping to an arbitrary point (time travel).

## Feature list

### v1 — Core engine (`@miraiclip/core`) ✅ shipped 0.1.0 (2026-09-05)
- Project state store (Zustand vanilla) with JSON serialization/deserialization and schema versioning/migration
- Command dispatcher: validation, execution, typed errors, transactions/batching
- Command catalog: project, track, clip, playback-position, and asset commands
- Clips: video, audio, image, text; add/remove/move/trim/split/duplicate/property-set
- Tracks: create/remove/reorder/rename, mute/solo/lock, type constraints
- Asset registry: register media sources with metadata (duration, dimensions, fps) — no decoding in core
- Undo/redo with full history and transaction grouping
- Patch emission (JSON patches) + event system (state change, selection, playhead, history events)
- Timeline math utilities (µs/frames/timecode, snapping, gap/overlap helpers)
- Custom command registration API
- Runs in browser and Node (headless, fully unit-testable)
- TypeScript-first: exported types, command schemas (Zod or JSON Schema)

### v2 — Rendering & playback (`@miraiclip/renderer`) — current

Decisions (settled 2026-09): **PixiJS** compositor, **mediabunny** for demuxing (multi-container, µs-accurate seeking, WebCodecs-native; also covers v3 muxing), **WebCodecs-required** browser policy (capability check, no `<video>` fallback pipeline), v2.0 scope = **preview player with audio** (effects/transitions stay in v4). The renderer is framework-agnostic but browser-targeted; the core remains environment-agnostic.

Design review hardening (2026-09): the three layers below are **strict internal boundaries**, so later extraction is mechanical. (a) The compositor is a pure function of (document, timeUs) driven through a `Clock` interface — `AudioClock` for realtime playback, `StepClock` reused by v3 export, guaranteeing preview/export visual parity. (b) The media pipeline (demux/decode/cache) is renderer-independent — v3 export and thumbnail/waveform generation consume it directly; a central **MediaManager** owns one pipeline per *asset* (clips sharing an asset share it), a global frame/VRAM budget, a decoder-count cap with LRU release (Safari allows few hardware decoders), and abortable seeks so scrubbing cancels stale decode work. (c) Clip rendering goes through a **registry** (`clip.kind → renderer factory`, built-ins pre-registered) mirroring the core's custom commands — the v4 seam; this requires a clip-kind extension mechanism in core 0.2, since `Clip` is currently a closed union. Also settled: per-asset codec-capability errors are first-class (not just a global `isSupported()`); preview renders at a scaled resolution independent of project resolution (devicePixelRatio-aware); color is explicitly SDR/sRGB in v2 (HDR out of scope); silent projects fall back to the raw AudioContext clock; core is a peerDependency and a document `schemaVersion` bump requires a coordinated renderer release.

Architecture — three layers behind one `createRenderer(project, canvas, options)` API:

1. **Media pipeline** (per video/audio asset): mediabunny `Input` demuxes → `VideoDecoder`/`AudioDecoder` (WebCodecs) → a **frame cache** of decoded `VideoFrame`s around the playhead. Decoding runs in a Web Worker; frames transfer to the main thread. Cache policy: decode-ahead window (~1s forward, a few frames back), keyframe-aware seeking (seek to preceding keyframe, decode forward, drop until target), eviction by distance from playhead with a hard VRAM budget — `VideoFrame.close()` discipline is the critical correctness concern.
2. **Compositor**: a PixiJS `Application` whose scene graph mirrors `state.doc` — one container per track (z-order from `trackOrder`), one node per visible clip (video texture / image sprite / text). Subscribes to the core's patch events and applies granular updates (a `clip/move` patch touches one node, never a full rebuild). Renders on `requestVideoFrameCallback`-style ticks during playback and on-demand when paused (patch → single re-render).
3. **Playback controller**: the **AudioContext clock is the master**. Audio clips are decoded to `AudioBuffer`s and scheduled via Web Audio (volume, mute/solo from track state); video frames are selected by the audio clock's current time mapped to timeline µs. Play/pause/seek/scrub and a rate control; playhead position pushed back into the core via `setPlayhead` (ephemeral, no history).

v2.0 feature list:
- Asset loading from `src` (URL/File/Blob) with metadata probe (duration, dimensions, fps → validates the core's asset registry)
- Video, image, and text clips rendered with transform (position/scale/rotation/opacity), source trim, and track layering; audio clips mixed with clip volume and track mute/solo
- Frame-accurate play/pause/seek/scrub, loop, playback rate; `renderFrameAt(us)` for thumbnails/posters
- Reactive: any dispatched command updates the canvas via patches, including during playback and time travel (undo/redo just emits patches)
- Capability detection (`isSupported()`), typed error surface, `destroy()` that provably releases decoders, frames, and GL resources
- Testing: unit tests for cache/clock/MediaManager logic in Node (mocked WebCodecs); Playwright browser tests rendering deterministic fixture scenes with tolerance-based pixel compare (GPU output varies across machines)

Build order: (1) ✅ mediabunny demux + frame cache with tests (streaming decoder, frame-accurate settle-pattern seeks; **carry-over: decode still runs on the main thread — the planned Web Worker is not built**) → (2) ✅ Pixi compositor for image/text clips → (3) ✅ video textures from cached frames + `renderFrameAt`, verified in-browser on real long-GOP files (**carry-overs: Playwright golden-frame tests replaced by manual verification so far; VideoFrame→GPU direct upload still goes through a 2D canvas**) → (4) ✅ audio graph + audio-master clock + `createPlayer` transport (streaming-windowed decode, per-clip gain lanes with live mute/solo, ephemeral playhead; verified with sound on real files — occasional 1–2s frame stalls remain, attributed to main-thread decode contention) → (5) performance & hardening — **5a done**: the 1–2s frame stalls were re-diagnosed as **hardware decoder frame-pool exhaustion** (the cache held up to 120 undestroyed VideoFrames; decoders own ~8–16 output slots and stall when starved), fixed by converting every decoded frame to an ImageBitmap and closing the VideoFrame immediately (the canonical WebCodecs pattern); the same change gives **direct ImageBitmap→GPU upload**, deleting the 2D-canvas hop. **Web Worker decode is deferred with rationale**: decode already runs off-main inside WebCodecs, and after 5a the main thread does only cache bookkeeping and one texture upload per frame — re-open only if profiling shows otherwise. **5a addendum (4K60 hardening, user-verified smooth)**: profiling on bbb 4K60 exposed and fixed a chain of re-seek storms — re-seek decisions made deterministic in stream state (never decode timing); eviction detection via a contiguity watermark anchored at the seek target; FrameCache eviction changed to past-before-future (symmetric distance eviction discarded fresh decode-ahead, causing a re-seek every cache-capacity of playback); decode-ahead sized by measured frame spacing and the byte budget; proxy preview decode (`createWebCodecsDecoderFactory({ maxOutputDimensionPx })`) with fit-to-composition layout; dirty-flag rendering (paused scenes cost ~nothing); loud `onError` surfacing. Each fix carries a regression test proven failing against the previous code (53 total). **5b done**: golden-frame Playwright e2e (deterministic frame-index-as-color fixture; frame-exact mid-GOP seeks + displayed-frames-track-the-clock playback invariant; serial, against the built playground; CI job added), background-tab playback fixed (interval pump keeps decode/audio windows rolling while rAF is frozen), playground trailing-throttle UI fix. Scrub/cache tuning evaluated and closed with no further change — the deterministic re-seek redesign (watermark + past-before-future eviction + budget-adaptive ahead window) already removed every measured scrub pathology → (6) ✅ `@miraiclip/renderer@0.1.0` published to npm (2026-09-06; tarball verified: dist-only, core as peer dependency, ESM+CJS). **v2 complete.**

Open questions to settle during v2: worker↔main frame transfer strategy (transfer `VideoFrame` vs render-to-`ImageBitmap`); whether the Pixi dependency is `peerDependency` or bundled; WebGPU backend (Pixi v8 supports it) as an option flag now or later.

### v3 — Export ✅ (complete 2026-09-06, shipped as renderer 0.2.0)

Decisions (settled 2026-09-06, with user): export ships **inside `@miraiclip/renderer`** (shares the compositor/media pipeline seams; releases as renderer 0.2.0); **MP4 (H.264) + WebM (VP9)** both in the first cut, with per-browser codec probing (`canEncodeVideo`) and clear unsupported errors.

Architecture: the preview pipeline on a deterministic clock. A frame loop walks output frames (`renderFrameAt` awaits full-resolution decode — proxy decode off — and composites via the same Pixi backend onto an OffscreenCanvas at project resolution); each frame is handed to mediabunny's `Output` (`CanvasSource.add(t, dur)` — its promise IS the encoder/writer backpressure, which is what makes export faster-than-realtime without unbounded memory). Sequential monotonic time = zero re-seeks. Audio is mixed offline via `OfflineAudioContext` reusing AudioEngine's scheduling math (gains, trims, mute/solo), then added via `AudioBufferSource`. Progress events (phase + framesDone/totalFrames), cancellation via `AbortSignal`, quality presets mapped to mediabunny `QUALITY_*` with bitrate override.

Known risks (grilled): offline audio memory ~230MB/10min stereo (chunked rendering is the v3.x escape hatch); many-asset timelines can thrash the decoder LRU cap (acceptable at v3 scope); H.264 encode unavailable in some browsers → probe + error, e2e uses VP9/WebM (Playwright Chromium has no H.264).

Build order (1–4 ✅ complete 2026-09-06; 71 headless + 5 e2e tests green): (1) export orchestrator + `ExportSink` interface, headless-tested with fakes (frame walk, rounding to exact composition end, progress, abort mid-run, sink error propagation) → (2) offline audio mix (scheduling plan headless-tested; OfflineAudioContext adapter) → (3) mediabunny sink adapter + presets + codec probing → (4) playground Export button + closed-loop golden e2e (export the frame-index fixture, re-import with our own demuxer, assert frame colors) → (5) docs + changelogs ✅; (5) docs + changelogs ✅ → user-verified in real Chrome (MP4 High, 4K60 source: no judder, ~1.5× realtime after pipelining) → changeset added for 0.2.0 (publish via the changesets flow). Post-ship perf round: pipelined encoding (bounded encoder in-flight window, `encodeAheadFrames` default 4; sinks capture synchronously — an explicit `ExportSink` contract), unclamped MessageChannel macrotask in `waitForFrame` (nested setTimeout's ~4ms clamp capped exports at ~60fps), playground output-fps select (Source/30/24).

### v3.x — Server-side export ✅ (complete 2026-09-06, shipped as @miraiclip/server-export 0.1.0)

Decision (2026-09-06, with user): build after 0.2.0 ships. Approach: **the same `exportProject` in headless Chromium** (Playwright), NOT a native Node pipeline — rebuilding decode/composite/encode on ffmpeg + a software canvas forfeits preview/export parity, which is the product guarantee. A new package (working name `@miraiclip/server-export`) exposes `exportProjectFile(docJson, options)` + a CLI: launch Chromium, load a minimal harness page, hydrate the project from JSON, run `exportProject`, write the bytes to disk. The CI e2e suite already proves this exact shape works headlessly (SwiftShader software GL included). Knowns to solve: (a) a clean hydrate-from-doc-JSON entry point in core; (b) asset `src` resolution on the server (URLs, or the harness serves local files); (c) MP4 needs codecs-enabled Chrome — stock Playwright Chromium is WebM-only; (d) throughput measurement on software GL. Built 2026-09-06 (14 tests green, incl. real-Chromium integration + CLI smoke): `packages/server-export` — `exportProjectFile` + `miraiclip-export` CLI; harness bundle self-contained (core+renderer baked in via tsup iife, version-locked); loopback static server with Range support (media decode seeks by byte range); asset resolution (explicit map → http(s)/data pass-through → assetsDir); browser resolution executablePath → MIRAICLIP_BROWSER → system Chrome via playwright-core (no install-time download; real Chrome carries H.264 — free Chromium is WebM-only); SwiftShader default on Linux; progress/abort forwarded across the process boundary (ServerExportAbortedError); hydration was free — `createProject` already accepts a ProjectDocument. CI runs the integration in the e2e job (Playwright's Chromium via MIRAICLIP_BROWSER). Explored & rejected: Obscura (Rust engine, no WebCodecs/WebGL/WebAudio); native Node/ffmpeg (parity loss); note Remotion's path validates ours (Chromium → Thorium → Chrome Headless Shell for codecs). User-verified 2026-09-06 (CLI + API, incl. MP4 via real Chrome). Release prepped: private flag dropped, changeset added (minor → 0.1.0), docs banner removed; publishes via the changesets flow. Also in v3.x: chunked offline audio rendering (memory).

### v4 — Creative features (planned 2026-09-06, with user)

Decisions (settled with user): captions are **reels-style karaoke** — word-level timing with active-word highlight styles; plain SRT/VTT subtitles import onto the same model as a special case. Transitions ship as **dissolve + a small set** (cross-dissolve, dip-to-black, dip-to-white, wipe, slide) — one shader-blend mechanism, ~5 built-ins proving the registry generalizes. Effects are **built-ins only** in v4 (color adjust, blur, chroma key); the public `registerEffect()` API freezes in v4.x after the internal shape survives real use.

Architecture: keyframes are the foundation. Core grows a pure evaluator — `evaluateClipAt(clip, timeUs)` — over per-property keyframe lists (`animations?: Partial<Record<prop, Keyframe[]>>` on ClipBase; props: transform x/y/scale/rotation/opacity + volume on a/v clips; `Keyframe = { timeUs (clip-relative), value, easing }`, easings: linear, hold, easeIn/Out/InOut). The compositor evaluates per tick and export inherits animation for free (same compositor). Effects: per-clip `effects?: [{ id, kind, params, enabled }]` stack resolved through an internal renderer registry (mirrors the clip-kind registry — the v4 seam) rendering as Pixi v8 filters; chroma key (color, similarity, smoothness, spill suppression) is one registry entry, not a subsystem. Transitions: doc-level `{ id, trackId, fromClipId, toClipId, kind, durationUs, params }` validated against adjacency + available trim handles; the compositor renders BOTH clips inside the window and blends via a transition-registry shader. Captions: new `caption` clip kind (`words: [{ text, startUs, durationUs }]` + style preset) — needs core 0.2's clip-kind extension mechanism (Clip is a closed union today); SRT/VTT parse is a pure core utility importing onto the karaoke model (one word-group per cue).

Design review hardening (grilled 2026-09-06, with user): (a) **easings are stored as cubic-bézier control points** — the named presets (linear/hold/easeIn/Out/InOut) are sugar expanding to béziers; an enum-only schema is a future migration. (b) **Keyframes anchor to the clip's visible start and are KEPT when a resize orphans them** (inactive past the end) — never scaled or deleted implicitly. (c) **Length-denoting effect params are normalized to composition units** (converted to pixels at render scale) — absolute pixels diverge between scaled preview and full-res export, a parity bug by construction. (d) **Video transitions apply an equal-power audio crossfade** over the same window by default, implemented in the shared audio mapping module so live playback and the offline export mix cannot disagree; animated volume ramps via linearRampToValueAtTime (per-chunk stepping = zipper noise). (e) **Built-in effect/transition param schemas live in core as Zod** so `keyframe/set`, `effect/add`, and `transition/add` all enter the AI command catalog as JSON Schema — the LLM-drivable editor story survives v4 intact. (f) Transitions use the **adjacent-clips + trim-handles model** (Premiere-style, centered on the cut; `alignment` param reserved), never physically overlapping clips. (g) Effects apply **pre-transform, in sRGB** (the core's explicit SDR policy); order within the stack is the array order. Scope settled with user: **font assets in v4** (asset kind "font" → FontFace; renderer loads before first render, export harness awaits document.fonts.ready — server exports must never fall back to default glyphs); **caption import = SRT/VTT (even word-split fallback) + ASR word-timestamp JSON** (Whisper-shape — real karaoke needs word times); **effect-param keyframes deferred to v4.x** (each effect instance's schema reserves room for its own keyframes).

Known risks (grilled up front): (a) **same-asset transitions break the one-pipeline-per-asset invariant** — two decode positions live at once; MediaManager needs a scoped second pipeline for the overlap window, counted against the decoder cap; (b) transition validation needs trim headroom — the outgoing clip must have source media past its visible end (text/image clips extend trivially); (c) caption fonts in the export harness must be loaded (FontFace readiness) before frames render or early frames draw fallback glyphs; (d) filter GPU cost on 4K preview — filters render at composition resolution (proxy decode already caps input); (e) keyframe evaluation must not allocate per tick (reused output objects).

Build order (docs + both changelogs at every step, per the house rule):
1. ✅ (2026-09-06; 60 core tests, all downstream suites green) **Core 0.2** — animation schema + keyframe commands (`keyframe/set|remove|clear`) + pure `evaluateClipAt` with easings (binary search, alloc-free); effects field + `effect/add|update|remove|reorder`; transition entity + `transition/add|update|remove` with invariant validation; clip-kind extension mechanism; **font asset kind**; `caption` clip kind + SRT/VTT parser (even word-split) + ASR word-JSON import command. Everything headless-tested (evaluator/easings are pure math).
2. ✅ (2026-09-06; 80 renderer tests, 8 e2e) **Renderer: animation** — compositor evaluates keyframes per render into reused scratch (setPlacement contract: object may be reused; Pixi video node copies); volume rides linear gain ramps via shared `volumeAutomation` (live engine re-issues per pump on the output clock; offline mixer schedules the same points — parity by shared math; hold segments pinned pre-jump; planAudioJobs keeps animated zero-static-volume clips). Golden e2e: opacity ramp = exact pixel values; export e2e: baked volume fade = expected RMS decay.
— OPEN INVESTIGATION (parked 2026-09-06): a single ~8ms full-canvas black frame at clip time ~1.29s on the user's machine (4K60 bbb, 120Hz display), deterministic across 8 probe runs. Ruled out via console probes: NOT stale-frame-through-opacity (transport hold fixed those), NOT video-node-only (text overlay blacks out too), NOT a failed render (dip frame has normal counters: 1 clear, 1 draw, 1 upload, no GL error, no exceptions) — a good-faith render produced black. Next steps when resumed: Test A (no keyframes — is it pre-existing and merely noticed under the ramp?) and Test B (start at 10s — elapsed-anchored ≈ decode/budget machinery vs content-anchored ≈ bbb.mp4 stream peculiarity at 1.29s). Console probe snippets are in the session log; the transport-hold + double-reseek fixes from this hunt already shipped.

3. ✅ (2026-09-06; 87 renderer tests, 11 e2e) **Effects + chroma key** — internal kind→filter registry (ColorMatrixFilter, BlurFilter, custom GLSL chroma key: CbCr distance + smoothstep + spill suppression, premultiplied-correct); `SceneNode.setEffects` pushed on every clip sync; in-place param updates (no recompiles); VideoClipAdapter forwards to the inner node (wiring gap caught by e2e). Fixture e2e-key.webm (red box on green); e2e: key on/off round-trip, grayscale/black extremes, and the keyed EXPORT re-decoded and pixel-asserted.
4. ✅ (2026-09-12; 106 renderer tests, 16 e2e) **Transitions** — pure timing module (window centered on the cut; equal-power cos/sin audio ramps clamped to each clip's own bounds — true overlapped audio sourcing is v4.x); MediaManager lane-scoped pipelines (`acquire(assetId, src, lane?)`) so both clips of one asset decode two positions through the overlap, upgraded one-way per participating clip; no render-textures needed — crossDissolve = incoming-on-top at alpha p (in-over-out ≡ the mix), wipe = composition-space mask reveal, slide = position offset, dips = a lazy full-frame overlay quad (alpha 1−|2p−1|, opaque exactly at the cut — dips need no out-of-bounds rendering at all); blend kinds render both clips through the window from source headroom (extended visibility + extended decode); audio crossfade rides the SAME `volumeAutomation` path as keyframed volume (quarter-point samples of the equal-power curve) — live and export parity by shared math. Playground Transitions tab: cards split at the playhead, jump the incoming side 1s ahead in the source (a visible cut — a continuous split would make the transition invisible), and bridge it. Golden e2e (5): dissolve blends the exact frame-index colors at p=0.25 and past the cut, wipe shows two different frames at once, slide's incoming edge sits at the computed offset, dip is pure black exactly at the cut, and the dissolve is re-decoded out of an exported file (parity, not assumption).
5. ✅ (2026-09-12; 114 renderer tests, 18 e2e) **Captions** — pure layout/timing module (`captions/layout.ts`: greedy word wrap at 80% comp width with per-line centering, block-center-relative word positions for anchor-0.5 placement; `captionProgress` = containing word + started count; `wordAppearance` per preset — plain/highlight/karaoke-progressive/pop-scale). Pixi caption node: one Text per word measured for layout, optional rounded background box, per-word emphasis dirty-gated on progress (style writes only at word boundaries). Font assets: `loadFontAssets(doc)` turns `kind:"font"` assets into document FontFaces (module-global dedupe, failed loads log and fall back) — wired into the live player (re-runs on asset patches; a finished load triggers the new `Compositor.resync()` since glyph metrics changed) AND into `exportProject` before the first frame, so server exports never rasterize fallback glyphs. Playground Captions tab (karaoke/highlight/pop/boxed cards drop a word-timed demo line at the playhead). e2e by color census (pure white + pure blue — colors the fixture cannot produce): karaoke keeps passed words lit and the caption vanishes with its clip; highlight's lit word flips sides of the canvas at the word boundary (20×-majority assert — a wide glyph sheds a few dozen pixels across the midline).
6. ✅ (2026-09-12) Shipped as a coordinated release: core 0.2.0 + renderer 0.3.0 + server-export 0.1.1 (dependency bump — rebuilt harness), published via the changesets flow and verified on npm. **v4 complete.**

### v5 candidate set — user-directed additions (2026-09-22, with user; shaping notes, nothing committed to build order yet)

Alongside the standing candidates (@miraiclip/react + example editor, templates, programmatic clips, locked regions) the user added four feature directions. Initial shaping:

**1. AI commands interface. ✅ BUILT 2026-09-22 (core `src/ai.ts`; 13 unit tests; docs "AI Integration" page; unreleased — first core change since 0.2.0, changeset queued as minor → 0.3.0).** Original shape below; as built, everything landed IN core (zero new deps — the module is pure plumbing over engine + errors, so the planned thin `@miraiclip/ai` package would have held nothing): (a) `toToolDefinitions(catalog, {style, mode, descriptions})` — Anthropic tool_use (default) and OpenAI function-calling shapes; `mode:"dispatch"` collapses the catalog into ONE `miraiclip_dispatch` tool `{type: enum, payload}` for tool-count-constrained hosts (built for the MCP item, which consumes this); tool names sanitize `/`→`_` (`toolNameForCommand`/`commandTypeForTool` round-trip — LLM tool-name rules forbid slashes); per-command descriptions for all 25 builtins, caller-overridable. (b) `tryDispatch` → `CommandResult` with `CommandFailure {kind: unknown-command|invalid-payload|rejected}` mapped from the existing typed errors (validTypes / per-field issues / rejection code — the structured-errors work from v1 paid off here: this was pure translation); non-command errors still throw. `applyCommands` = one `project.transaction` — all-or-nothing (engine rollback), one undo step, `failedIndex` reported. (c) `describeProject(doc, {maxClipsPerTrack=50})` — deterministic µs-primary summary (header states the unit; assets/tracks/clips/transitions sorted; long tracks middle-elided). (d) Docs page `docs/ai-integration.md` (weight 8; compat/roadmap/changelog bumped) — recipe-first: tools → execute-with-self-correction loop → atomic plans → prompt summary → whole-loop example; command-catalog page cross-links. Original shape: (a) `toToolDefinitions()` — export the command catalog as LLM tool definitions; (b) an LLM-loop-friendly dispatch: structured, machine-readable validation errors so an agent can self-correct, plus transactional batch apply with rollback on first invalid command; (c) `describeProject(doc)` — a compact, token-efficient state summary for prompts; (d) docs recipe with a runnable example.

**2. MCP integration (Claude, Codex, and other agents). ✅ BUILT 2026-09-22 (packages/mcp; unreleased — changesets queued: mcp minor → 0.1.0, renderer minor → 0.5.0, server-export minor → 0.3.0).** As built, three layers, user chose "Full: edits + export + preview_frame" over phased: (a) **renderer `renderProjectStill(project, {timeUs, width?, height?, type?})`** (`src/export/render-still.ts`) — exportProject's exact setup (canvas/manager/videos/backend/compositor/fonts) minus sink; `videos.renderFrameAt` + `canvas.convertToBlob`. Building it EXPOSED A SHIPPED BUG: the Compositor constructor + settings-patch path unconditionally `backend.resize(doc.settings)`, so **`exportProject`'s width/height output option was silently ignored through 0.4.x** (verified: 640×360 comp exported at 320×180 produced a 640×360 file). Fix: `CompositorOptions.outputSize` + optional `SceneBackend.setOutputSize` — Pixi backend renders the canvas at OUTPUT size and scales the stage, `compSize` stays composition-space so placement/masks/caption layout are untouched; exports and stills both pass it; verified 320×180 request → 320×180 file; all 123 renderer unit + 23 e2e green. (b) **server-export `createRenderSession()`** (`src/render-session.ts`) — ONE warm Chrome+harness page across calls (~250ms first frame, ~94ms warm); doc passed PER CALL so the session outlives edits; ONE mutable files Map held by the harness server by reference, re-filled from `resolveAssetSources` per render so new assets become servable without restart; FIFO queue serializes renders and a failed render never wedges it; harness gained `__miraiRenderStill` (protocol + main). 3 integration tests. (c) **`@miraiclip/mcp`** — low-level SDK Server API (`setRequestHandler` with our own JSON Schemas — core already validates, NO zod double-layer; SDK ^1.30.0, zod-version-agnostic); 10 tools: get_state (describeProject; include_json opt), dispatch + apply_commands (schemas from core's DISPATCH-MODE `toToolDefinitions` so the type enum always matches the live catalog — item 1 dogfooded), undo/redo (return fresh summary), list_commands/get_command_schema (token-lean catalog access), preview_frame (MCP IMAGE content, default downscaled to ≤768px for context economy; dead-browser restart-once), export (wraps exportProjectFile, streams to disk, out resolves against project dir), get_project_json. `ProjectSession`: load-or-create project JSON, ATOMIC autosave (tmp+rename) after every successful mutation, lazy warm renderer, assetsDir defaults to the project file's dir. CLI `miraiclip-mcp --project --assets --width/height/fps --browser`; ALL logging to stderr (stdout is the protocol). 7 tests via real SDK Client over InMemoryTransport (tool list, autosave, three failure kinds, batch rollback + one-step undo, catalog; browser-gated: preview_frame IHDR 768×432, export webm) + a real stdio round-trip smoke. Dev note: `node dist/cli.js` in-repo fails on the workspace src-exports pattern (deps resolve to .ts) — `npx tsx src/cli.ts` for dev; the published package resolves to dists and is what clients run. Docs: docs/mcp-server.md (weight 9; compat/roadmap/changelog bumped; ai-integration cross-links). Original shape (as planned): generic dispatch tool over the catalog, get_state summary, undo/redo, preview_frame via a harness frame renderer, export via server-export; session = project JSON on disk + assets dir; dogfoods item 1.

**3. Custom animation/effects/transitions. ✅ BUILT 2026-09-22 (renderer; unreleased — changeset queued: renderer minor → 0.6.0).** As built: `registerEffectRenderer(kind, factory)` (effects/pixi-effects.ts — the internal FACTORIES map became a public Map registry; built-ins colorAdjust/blur/chromaKey register through the same `EffectRendererFactory` contract: params → `ActiveEffect {kind, filter, update-in-place}`; duplicate kinds throw, built-ins included) and `registerTransitionRenderer(kind, renderer)` (NEW transitions/registry.ts — `TransitionRenderer {rendersBothClips, frame(role, progress, params, {compositionSize}) → TransitionFrameEffects | null}` composing four primitives: opacity multiply, directional reveal, pixel offset, full-frame overlay; the compositor's per-kind switch in renderAt was REPLACED by registry lookup and all five built-ins re-expressed through the contract — the 5 golden transition e2e prove the re-expression pixel-identical; timing.ts `rendersBothClips` now answers from the registry, so decode headroom extension and the equal-power audio crossfade apply to custom kinds automatically; unknown kind = hard cut, no throw). 7 new unit tests (extensibility.test.ts: duplicate rejection, custom overlay + custom blend through the real compositor via FakeBackend, hard-cut fallback, effect registry round-trip + schema validation via core registerEffectKind); 130 renderer unit + 23 e2e green. Scope decision on "custom animation": standard keyframes (x/y/scale/rotation/opacity/volume) already apply to EVERY clip kind — placement math is kind-agnostic — and custom clip nodes get `tick(clip, timeUs)` per frame for param-driven animation; **effect-param keyframes deferred** (needs a core schema change: keyframe/set property is a fixed enum — design its own build). Boundaries documented: renderers are functions → server/worker export stay built-in-only (same rule as factories). Docs: rendering/extensibility.md (weight 5; effects/transitions/roadmap cross-link). Original shape: (finish the extensibility story) Core registration already exists (`registerClipKind`/`registerEffectKind`/`registerTransitionKind`) and clip-kind FACTORIES reach the renderer; the gaps are renderer-side: public `registerEffectRenderer` (effect kind → filter/GLSL factory — the internal kind→filter registry exists, this makes it public with a stable contract), `registerTransitionRenderer` (kind → composite behavior over the existing timing module: alpha/mask/offset/overlay primitives), and animatable params for custom kinds (keyframes already evaluate arbitrary properties; define the contract for custom nodes to consume them). Known constraint to design around: server export bundles a fixed harness — custom renderers are functions and can't cross, so server-side custom kinds need a user-supplied harness bundle (document as out-of-scope initially, revisit). Subsumes the old "public registerEffect" roadmap line; effect-param keyframes fold in here.

**4. HTML-in-canvas / parameterized videos (the "hyperframes" direction). (a) HTML CLIP KIND ✅ BUILT 2026-09-22 (unreleased; changesets queued: core minor → 0.4.0, renderer minor → 0.7.0, server-export patch for the rebuilt harness). (b) parameterized documents (@miraiclip/templates) remain.** As built, exactly per the settled design below: core `HtmlClip` (`{template, params, widthPx?, heightPx?}`, clip/add union arm, `clip/set-property {params}` merge, TRACK_ACCEPTS.video, reserved built-in kind, 4 core tests); renderer `html/rasterize.ts` (data-URL foreignObject + 2D-canvas launder, LRU-free promise cache keyed (template,params,w,h), resource cache for @font-face/`asset:` data-URI inlining, param HTML-escaping, worker throws a clear message) + `PixiHtmlNode` (sprite, texture swap on key change, old texture destroyed) + **`SceneNode.whenReady?()`/`Compositor.whenReady()` readiness gate awaited by exportProject/renderProjectStill — this also fixed the LATENT image-asset export race (image textures loaded fire-and-forget; an export started immediately could bake empty sprites into early frames)**; `exportViaWorker` renders html clips too (pre-raster on main, bitmaps transferred — see the worker follow-up below, ✅ BUILT 2026-09-23). VERIFIED: 3 e2e (live composite pixel-exact, param re-raster in place, baked into exported files FROM FRAME 0 — the readiness gate's proof), server exportProjectFile frame-0 ffmpeg YUV check ≈ magenta, warm render-session still, smoke `ex-html` red-census; suites 77 core + 135 renderer + 18 server-export + 7 mcp + 26 e2e. Docs rendering/html-clips.md (weight 6); examples page "HTML Clips" section (lower third + live-params countdown). Original design notes (all held):

*Feasibility findings (POCs run in Chromium 141, SwiftShader — the export environment):*
- **Rasterization recipe that works TODAY: HTML → SVG `foreignObject` → `data:` URL → `HTMLImageElement` → GL texture.** CRITICAL empirical finding: a foreignObject SVG loaded via **blob: URL TAINTS the canvas** in Chromium 141; the SAME markup via **`data:` URL is untainted** (verified: 2D getImageData, WebGL texImage2D both clean). Second wrinkle: `createImageBitmap(img)` from that data-URL image carries a bogus cross-origin flag and FAILS GL upload — upload the `HTMLImageElement` directly, or launder through a 2D canvas → `createImageBitmap(canvas)` (both verified working). `createImageBitmap(svgBlob)` directly fails outright (no SVG decode).
- **Measured costs:** first 1920×240 rich raster (gradient/flex/shadow/emoji/alpha) 25.5ms; warm 640px re-rasters ~1.6ms each; template data URL ~0.7KB. Alpha survives (rgba a=204 verified). A raster cache keyed on (template, params, size) makes per-frame cost zero; param-driven re-raster is the only expensive path and it's ~2ms.
- **Native HTML-in-Canvas API (tier 2): Chrome origin trial 148–150** (`layoutsubtree` attr, `drawElementImage`, `texElementImage2D`, WebGPU `copyElementImageToTexture`). Per the WICG explainer it is **readback-safe by design** — "privacy-preserving painting" excludes sensitive content (visited links, autofill, spellcheck, subpixel AA) from the PAINT instead of tainting the canvas — and **`captureElementImage()` yields transferable `ElementImage` snapshots usable on OffscreenCanvas in WORKERS** (would close the worker-export gap). Cross-origin iframes excluded. Not a foundation (a year-ish from baseline; our Chromium 141 predates it) — it slots in as tier 2 behind the seam.

*Design decisions:*
- **`html` is a BUILT-IN clip kind, not a userland custom kind.** The decisive property: its payload is DATA (`{ template, params }`), not functions — so unlike custom factories it crosses process/thread boundaries: server export renders it (the harness page is a real Chrome document), MCP agents can dispatch it (`preview_frame` shows it), and template documents serialize it. Schema sketch: `clip/add { kind: "html", template: string, params?: Record<string, string|number|boolean>, width?, height? }` — `{{param}}` substitution, params merged via `clip/set-property` (a param change = re-raster).
- **Rasterizer seam** `HtmlRasterizer: (markup, widthPx, heightPx, resources) → canvas/ImageBitmap` — tier 1 = foreignObject/data-URL (above), tier 2 = native API by feature detection, injectable (a user could plug satori/resvg for cross-env-deterministic server rendering). Node = a Pixi sprite over the raster texture → **everything existing composes for free**: keyframes (placement is kind-agnostic), effect stacks, transitions, reveal masks, output scaling.
- **Fonts: SVG-as-image is an ISOLATED document** — document fonts (our `loadFontAssets` FontFaces) do NOT reach it. Font assets referenced by an html clip get inlined as `@font-face` `data:` URIs into the raster (we hold the bytes via asset src; fetch+base64, cached per asset). Same rule for images inside templates: auto-inline same-project image assets as data URIs; external URLs don't load in SVG-as-image, period (verified: the SVG loads, the external <img> paints blank).
- **Feature envelope (tier 1):** full CSS layout (flex/grid), gradients, shadows, radius, filters, emoji, BiDi text, transparency — YES. Scripts, live CSS animations, video/iframe/canvas inside, external resources — NO (static deterministic snapshot per (template, params, size), which is exactly what export parity wants). Content animates via param changes (re-raster, ~2ms) at word/beat cadence, not 60fps full-frame; transform/opacity/scale/rotation animate the raster via existing keyframes for free.
- **Worker export:** ✅ BUILT (2026-09-23) via the pre-rasterize-on-main path: `exportViaWorker` calls `collectHtmlRasters(doc)` (dedupe by raster key; params are static per doc) and transfers the ImageBitmaps with the start message; the worker installs them via `provideHtmlRasters` and `rasterizeHtml` serves from the store before its DOM check. Tier-2 `captureElementImage` remains the future zero-main-work path.
- **Determinism/corpus:** raster is a pure function of (template, params, size) within one environment; text AA differs across platforms → corpus asserts census/structural checks (color presence, region bounds), never exact pixels. Chromium-family remains the rendering target; note Safari taints foreignObject canvases (renderer is Chromium-targeted anyway).
- **(b) Parameterized documents** (= @miraiclip/templates): a template project doc + `data` payload → hydrated doc (text/media/color/param substitution) → export; html clips are the primary consumer (batch/personalized video, server-side at scale via MCP/server-export). Builds directly on (a); separate build.

*Original shaping notes:* Two layers. (a) An `html` clip kind: author overlays (lower thirds, cards, charts) as HTML/CSS templates with parameters, rasterized into the compositor — implementation ladder: SVG `foreignObject` snapshot (works today; same-origin/no-external-resource limits; cache the raster, re-rasterize only on param change), the experimental HTML-in-canvas APIs where available, and on the server the headless-Chrome harness can rasterize DOM directly. Animated via the existing keyframe system (transform/opacity animate the rastered node; param-driven re-raster for content changes is the expensive path — design a dirty/cache contract). (b) Parameterized documents: a template project + a `data` payload → hydrated doc (text/media/color substitutions) → export; this IS the planned @miraiclip/templates, now motivated concretely (batch/personalized video generation, server-side at scale via the MCP/server-export path). Biggest new surface of the four; needs a corpus story (HTML raster determinism across environments is NOT pixel-exact — decide what the corpus asserts, likely structural/census checks not exact pixels).

*Sequencing observation (not decided):* 1→2 are small-to-medium and compound (2 consumes 1); 3 extends proven seams; 4 is a major feature with parity/testing implications. All four align with adoption momentum from the launch posts; the v4.x hardening list (corpus wave 2, long sources, recovery, transition preview smoothness — designed and pending) still stands and is unaffected.

### Future roadmap exploration (2026-09-06, with user)

**Production readiness — honest state and the stress suite to close the gap.** ✅ **Stress tier shipped (2026-09-12)**: `pnpm stress` + nightly `.github/workflows/stress.yaml` (metrics artifact `apps/playground/stress-results.json`), docs "Production readiness" page with measured numbers. Five scenarios, all as planned: (a) ~150 clips / 10 tracks clock-tracking playback + deep-seek recovery (wrap-aware lag on the clip sawtooth; `STRESS_MAX_LAG_FRAMES` default 30 sized for SwiftShader decode-behind — golden e2e keeps 12 on the single-clip case); (b) `STRESS_MINUTES` (default 5) synthetic export asserting **residual** heap bounded (heap minus pro-rated output share — BufferTarget accumulation is honest growth, a per-frame leak is not; fixture has no audio so this bounds the video path); (c) 20-asset churn over the 4-pipeline cap + 30-seek deterministic scrub storm, gated on progress + exact (±1) paused probes; (d) 60s throughput benchmark with `STRESS_MIN_FPS` floor + optional 4K→1080p behind a local fixture (`stress/make-4k-fixture.sh`); (e) `STRESS_PARALLEL` (3) server exports gated on interference (batch < N×solo×1.15 — NOT speedup: CPU-saturated boxes can't parallelize encode), each output independently re-parsed. **The suite caught two real bugs before shipping**: (1) pipeline-eviction blackout — clip adapters cached evicted (disposed) pipelines forever and every clip acquired eagerly at mount, so >cap distinct assets or ≥2 transitions (dedicated lanes) evicted the on-screen pipelines → permanently black, real hardware included; fixed with lazy acquisition + `VideoPipeline.isDisposed` re-acquire on tick/prepare (unit test: 4 assets through a 2-pipeline cap, return to evicted clip, exact frame). (2) SwiftShader GPU shared-image growth — decoder+encoder running simultaneously under software GL retains one shared-image per captured canvas frame (~2.7MB each; CDP memory-infra isolated the pair as the trigger, all library-side closes correct), ~4GB per 5-min export; fixed by `cpuCapture` in `createMediabunnySink` (VideoSampleSource + 2D mirror) auto-set by `exportProject` via the new exported `isSoftwareWebGL(canvas)` — backend now created before the sink so the GL context exists for detection; real GPUs keep zero-copy `CanvasSource`. Known ceilings stand (documented on the docs page, not asserted): offline audio mix ≈ 230MB per 10min stereo (chunked rendering is the planned v4.x fix), `BufferTarget` holds the whole encoded file, server-export's base64 hop transiently doubles peak memory. **External review accepted (2026-09-13): the numbers are regression benchmarks, not production-readiness proof** — docs page reframed accordingly (JS-heap-only caveat; hardware exports use a different capture path the memory numbers don't cover; pipeline cap stated as an engine default, not a browser limit; hour-long exports called impractical until chunked audio + streaming output land — those two are RELEASE REQUIREMENTS for hour-scale export support, not optional). **Next benchmark iteration (v4.x, from the review)**: real-device playback percentiles (p95/p99 presentation lag ≤1/≤2 frames proposed targets, dropped/repeated frame counts); warm/cold seek latency p95/p99 (≤150/≤300ms proposed) + correctness under overlapping seeks; 60–120min sources + a full 1-hour output; process/GPU-level memory + repeated open/edit/export/close cycles; decode-back correctness through the end (timestamps, frame counts, transitions, captions, A/V sync); recovery (seek/export cancel + retry, decoder failure, missing media, GPU context loss); sustained server concurrency (long jobs, increasing parallelism, p95 completion, failure rate). many-clips now records lagP95Frames (first run: p95 19 frames under swiftshader, median 2).

**Production target spec (adopted 2026-09-13, user-defined).** These are Miraiclip's proposed engineering targets — there is no industry-wide certification for a video-engine; these are OUR product targets, not published claims about other editors. Headline: smooth 1080p editing, frame-accurate output, bounded memory during hour-long exports, ≥99.9% successful exports on supported workloads.

*Workload definitions (targets are meaningless without them; total clips, distinct assets, and simultaneously-visible videos are SEPARATE benchmark dimensions):*
- **Reference device**: a named 16GB-RAM laptop with integrated graphics; every result records CPU/GPU/OS/browser versions.
- **Standard project**: 1080p30 output; 3 simultaneously visible video sources; captions + transitions + transforms + mixed audio.
- **Long project**: 60-minute timeline over ≥5 distinct hour-long source files.
- **Heavy project**: 4K sources, 6 simultaneously visible videos, multiple effects — tested separately with an explicitly declared proxy-preview mode.

*Preview/editing targets (p95 = 95% of measured operations meet the threshold):* sustained playback holds project cadence measured on PRESENTED frames (not render-loop callbacks); missed presentation deadlines <0.1% in steady playback (stalls reported separately); presentation timing error p95 ≤1 frame / p99 ≤2; zero unexplained freezes >100ms in the standard benchmark; warm play-start AND warm seek p95 ≤150ms / p99 ≤300ms (a seek counts only when the CORRECT target frame is presented — moving the playhead over a stale frame is not a completed seek; rapid scrubs coalesce, stale completions must never overwrite newer results); cold local seek p95 ≤500ms (includes asset open + decoder init); timeline-manipulation feedback p95 ≤50ms; scrub preview freshness p95 ≤100ms with local proxies; preview A/V sync |offset| ≤20ms p95 / ≤40ms p99. Frame budget context: 33.3ms at 30fps — keep headroom. INP ≤200ms @p75 is a useful extra signal but does not measure async seek completion.

*Export targets:* export-time ratio = wall-clock / output duration; standard 1080p30 ratio ≤1.0 on the reference device (stretch ≤0.5); heavy/4K gets a separately published profile (initial goal ≤2.0); success ≥99.9% of valid supported jobs (excluding user cancels) — statistically, ~3,000 zero-failure independent representative jobs bound failure <0.1% at 95% confidence, so repeating one tiny fixture proves nothing; output correctness 100% on the release validation corpus (frame selection exact to the timeline sampling rules incl. cuts/transitions; timestamp integrity — no unintended dropped/duplicated frames; A/V sync within one output frame across a full hour with no accumulating drift); cancellation acked ≤100ms with resources released p95 ≤2s; progress ≥1 update/s, never claims completion before finalization succeeds. Speed targets are only comparable at FIXED codec/bitrate/resolution/effects settings — faster-by-lower-quality is not equivalent. Quality via reference renders + color patterns + decoded comparisons; PSNR/SSIM/VMAF complement, no universal VMAF threshold across content types.

*Memory/reliability targets (starting budgets, not browser limits; JS heap alone is insufficient — measure process, workers, native codec allocations, GPU where available; WebCodecs treats CPU/GPU memory and codec handles as exhaustible resources):* standard editing ≤1GB incremental CPU-resident on the reference device (GPU separate); export working set ≤1.5GB incremental for the standard workload; media buffers bounded across 10→30→60-minute durations; encoded output STREAMED to a sink (never whole-file retention); audio mixed in bounded chunks (never whole-timeline PCM); no sustained retained-memory growth across 100 open/edit/export/close cycles after warm-up; 8-hour soak (edit/play/export) without crashes or unrecovered stalls; server: publish max concurrency preserving success/completion targets, queue overload rather than degrade running jobs.

*Five mandatory gates for the next release:* (1) replace the 30-frame playback tolerance with real-device presentation metrics; (2) demonstrate a complete one-hour export with streaming output + chunked audio; (3) pass frame-selection/timestamp/color/A-V-sync validation on decoded exports; (4) meet warm-seek targets during rapid seeks across distinct long source files; (5) publish results by device and workload, keeping software CI as a separate regression baseline. Every published result carries commit, device, browser, codec config, proxy mode, run count, percentiles, and peak memory. Gate-2 status: ✅ **DEMONSTRATED 2026-09-13, on the user's real hardware** — a full 60-minute timeline (2,641 clips, 86,400 frames, a complete hour of chunked audio) exported with streaming output in 2,276s wall (38 fps ≈ 1.6× realtime), 984.2 MB streamed, JS heap peak 165 MB, settled spread 91 MB (gate <150). Chunked audio + streaming **SHIPPED 2026-09-13 as renderer 0.4.0 + server-export 0.2.0** (changesets flow, verified on npm), verified by unit/e2e/stress; two prior cloud-sandbox attempts died to container restarts (~50 min and ~21 min in, memory flat both times) — long background runs don't survive Cowork sandbox restarts; the user's machine ran it in one shot.

**Gates 1+4 — device bench harness ✅ BUILT 2026-09-13 (software baseline recorded; real-device run pending on the reference laptop).** `pnpm bench` (apps/playground/bench/, playwright.bench.config.ts — real-GPU flags by default, `BENCH_SOFTWARE=1` pins SwiftShader for the CI baseline). Everything measures ACTUALLY PRESENTED frames via the pixel oracle sampled by an IN-PAGE rAF loop (never per-sample round trips, never render-loop callbacks). New 1080p30 fixture (`bench/make-bench-fixture.sh`, local-only, `-g 30` — 1s GOPs bound seek decode like real editing sources; frame index extends into BLUE high bits since r/g saturate at frame 256). Scenario 1 (gate 1): the Standard workload (1080p base + 2 PIPs = 3 visible videos, karaoke captions, animated text, tone audio) → play-start latency, presented-frame lag p50/p95/p99, missed-deadline %, stalls >100ms, presented fps — recorded with DEVICE IDENTITY (GPU string, cores, platform, software flag) per gate 5. Scenario 2 (gate 4): warm seeks (12 software / 40 real), cold seeks into 6 never-opened assets (STAGGERED per-asset targets — identical expected frames let canvas leftovers read as instant false completions, caught by the baseline's impossible <2ms colds), rapid-overlap storm (last target wins, stale completions must never overwrite — held-frame re-check). Gates scale by device class: software = not-wedged floors + 60s seek headroom; real hardware = real floors. Device identity now also records vp9HwDecode (WebCodecs prefer-hardware probe) — a real GPU renderer string does NOT imply hardware decode (Playwright's bundled Chromium typically renders on the GPU while decoding VP9 in software), and published numbers must say which decode path they measured. **The bench caught its third real product bug (2026-09-13, first real-device run)**: presentation wedged at 9.6 presented fps with a 34s stall (lagP50 458 frames) despite playStart 67ms — the Standard workload's 2 PiPs reuse the base clip's ASSET at different trims, and outside transitions all three clips shared ONE pipeline, reversing its seek target every frame (each reversal clears the frame cache → wedge; the 'software can't decode 1080p×3' story for the 1.2fps CI baseline was WRONG — it was this thrash everywhere). Fix in renderer: prepare() detects timeline-OVERLAPPING clips of one asset among the relevant set and upgrades each to a dedicated pipeline (same mechanism as transitions; sequential splits/cuts keep sharing for continuity), plus two MediaManager LRU repairs the overlap exposed — pipelines are touched on every tick/prepare (LRU order used to freeze at acquisition, so the victim was often the clip longest on screen), and an actively-used pipeline is never evicted (cap temporarily exceeded instead of an acquire/evict livelock; `evictionIdleMs`, default 500ms, 0 = old hard cap). Unit-tested (overlap → both clips' exact frames; touch changes the LRU victim; live pipelines exceed the cap). Post-fix software baseline: presentation {playStart 264ms, presentedFps 2.1 (was 1.2), longest stall 706ms — no wedge, now genuinely decode-bound; sampler floor scaled: 3 dedicated 1080p lanes make a SwiftShader rAF ~400ms → >40 samples software / >100 real}; seek {initialColdOpenMs 435ms — now recorded, warm p50 743ms, colds 1.7–9.9s, rapid settles}. Second real-device run (post-fix): presentation PASSED on real hardware; the seek "failure" was the ORACLE, not the seek — the new diag showed frame 275 for expected frame 3 with the clock at target (275 = 3 + one g-quantum + one b-quantum; r matched exactly), i.e. the seek completed and the pixel read drifted. Root cause: the fixture was UNTAGGED — geq writes RGB, swscale converts with its default matrix, and every decoder then GUESSES the YUV→RGB matrix back; the guesses differ PER DECODER on one machine (the seek page's single decoder went hardware and read g/b +8..15 high; presentation's three dedicated lanes fell back to software decode, which happened to match the oracle — explaining pass+fail in one run). Fix: make-bench-fixture.sh now converts AND tags bt709/tv explicitly (`scale=out_color_matrix=bt709:out_range=tv` + stream tags; ffmpeg round-trip verified ±3 of exact quanta), and BOTH specs call assertPixelOracle() right after load — parked frame 0 must read exactly, else fail fast naming the measured rgb and the regenerate command instead of poisoning every seek with 15s timeouts. Lesson for every pixel-oracle fixture: an untagged video's colors are not portable across decode paths, even on one machine. **FIELD CRASH + FIX (2026-09-14).** User report: 257s 4K export (captions, text, animations, transitions) in the playground on renderer 0.4.0 pegged the CPU and HARD-RESTARTED the laptop. Built the crash-repro harness (`pnpm --filter miraiclip-playground repro:4k`, playwright.repro.config.ts): exports the playground way (buffered, source fps) while sampling OS-level per-process Chrome memory via `ps` beside frame progress + JS heap, with abort watchdogs (REPRO_MAX_GB total-RSS, REPRO_STALL_SEC progress) so a runaway can never take the machine down again — the recorded curve is the diagnostic. Cloud software run (40s 4K workload, 31 clips): GPU process plateaus ~3.4GB (SwiftShader working set, sawtooth, NOT a leak), but the JS heap stair-stepped 51→262MB for a 7MB output — one step per finished clip. ROOT CAUSE: mediabunny `Input`s cache up to 64MiB of fetched source ranges; our video/audio adapters' dispose() was a documented no-op ("GC handles it"), and ended clips' nodes retain their disposed pipeline → demuxer → Input → cache. Per-clip retention scales with SOURCE SIZE (≈6.5MB/clip on the 91MB fixture; up to 64MiB/clip on multi-GB sources, video lanes AND per-chunk audio jobs, as ArrayBuffers exempt from the V8 heap cap) — machine-exhaustion signature matching the report. FIX: both adapters now call `Input.dispose()` (frees the cache, cancels in-flight reads; pump treats teardown-canceled reads as termination). Verified: heap 79→147MB same run (stair-step gone; residual ≈2MB/clip is Pixi text/caption textures — clip-count-bounded, parked), 123 unit + 20 e2e + 21 corpus green. **SECOND FIELD CRASH (2026-09-22): WebM High, long 60fps source, real GPU.** A ~10-min 1080p60 movie exported via the worker (WebM High, source fps, streamed) grew Chrome past 20GB by frame ~2,800 → macOS OOM. Main-thread JS heap was FLAT (17MB — worker design working); the growth was process-level. Cloud repro at the exact config on the CPU-capture path (SwiftShader → cpuCapture): 10,798 frames, worker+streamed, Chrome flat ~3GB, renderer ~700MB, PASSED — so the runaway is specific to real-GPU `CanvasSource` capture feeding a SOFTWARE encoder (VP9 has no hardware encoder on macOS; his MP4/H.264-hardware exports never showed it). Same failure class as the SwiftShader capture leak — 'real GPUs unaffected' was true only at hardware-encoder drain rates. FIX: `exportProject` sets cpuCapture when `hasHardwareVideoEncoder(format, width, height)` (new probe, prefer-hardware isConfigSupported, errs toward CPU capture) is false — zero-copy capture only where hardware encoding drains it. Repro harness gained REPRO_FIXTURE/BARE/WORKER/STREAM/FPS knobs + a 1080p60 fixture recipe. PENDING: user verifies the same export on his M3 (expect flat memory; WebM exports will run at CPU-capture speed, which the software encoder already bounded). NEXT on the crash: user re-runs his real 257s export through `pnpm --filter miraiclip-playground repro:4k`-style guard or directly in the playground on the fixed build; playground should also gain a streaming save path (`showSaveFilePicker` → `target`) so long exports stop buffering the file in the page. NEXT: user re-runs `bash apps/playground/bench/make-bench-fixture.sh` (once — the tagged fixture) then `pnpm bench` on the reference laptop → real-device numbers → docs page gains a "reference device" section beside the CI baseline.

**Worker export ✅ BUILT 2026-09-14 (design below as adopted; decisions settled with user: both API layers, built immediately).** Shipped in Unreleased: `exportProjectInWorker` (auto-spawn, `workerUrl`/`worker` overrides, terminates what it spawned) layered on `exportViaWorker` (app-owned Worker, never terminated) + worker entry at the `@miraiclip/renderer/export-worker` subpath (tsup second entry `dist/export.worker.js`; dev exports point at the src .ts — Vite `?worker` import bundles it, used by the playground). Seams that made it small: exporter treats audio chunks as OPAQUE (timing tracked externally) so raw-PCM chunks needed no exporter change; `exportProject` gained `audioOverride` {hasAudio, mixChunk} (probe + mixer extracted as `probeCompositionAudio`/`compositionEnd`); the sink routes by FIRST chunk shape — `AudioBuffer`→`AudioBufferSource` (unchanged) or `PcmAudioChunk`→`AudioSampleSource` with running timestamps (f32-planar interleave); `loadFontAssets`' browser env now falls back to the worker's `self.fonts`; worker entry sets Pixi `DOMAdapter.set(WebWorkerAdapter)` before backend creation; driver mixes chunks on main via `mixCompositionAudio` and TRANSFERS the planes (copyFromChannel — an AudioBuffer's own storage is not transferable); `done` bytes transferred; streaming `target` RELAYS chunks through main with per-write acks (LESSON, caught by the user's first real save-picker run: `FileSystemWritableFileStream` is NOT structured-cloneable/transferable — only plain WritableStreams are — so the worker never receives a stream; the relay is one path for every stream, the ack carries backpressure, and chunk data is COPIED before transfer since the muxer may reuse its buffers; e2e now covers the streamed worker path with an in-page target). Playground Export button: worker + `showSaveFilePicker` streaming (partial file aborted on failure; buffered blob-download fallback; picker dismissal = clean cancel); `__mirai.exportProjectViaWorker` for e2e; vite `worker.format: "es"` (pixi's code-splitting forbids iife workers). Verified: 22 e2e green incl. 2 new (worker export frame-exact via native decoder + unity-gain audio through the PCM boundary + MAIN-THREAD rAF p50 at display cadence mid-export, where a main-thread export starves it; abort crosses the boundary), 21 corpus, 123 unit. Release shape: renderer MINOR (new API surface). Original design notes follow. Motivation: the export pipeline (decode → composite → capture → encode → mux) runs on the MAIN thread, so a running export pegs the tab's thread and the UI/preview lags (measured 97-98% CPU on the reference laptop). A DedicatedWorker does NOT reduce total CPU — same work, same cycles (plus small transfer overhead) — it frees the main thread, keeps the UI responsive, and usually finishes FASTER (no rAF/UI contention). A separate, cheaper knob for literally-lower CPU is pacing (idle-yield between frames, "background priority"); orthogonal, can ship with or without the worker.

*Feasibility — verified against the code (2026-09-14):* the exporter ALREADY renders into its own `OffscreenCanvas` (never the page's canvas), and WebCodecs decode/encode, `createImageBitmap`, `fetch` (http/blob URLs), and `FontFace`/`self.fonts` are all available in DedicatedWorkers — the entire VIDEO path is worker-compatible by construction. Pixi 8 runs in workers behind `DOMAdapter.set(WebWorkerAdapter)` (worker entry sets it before backend creation). `loadFontAssets` already takes an injectable `FontEnv` — add a worker env. The ONE window-bound piece is the audio mix: `OfflineAudioContext` and `AudioBuffer` are not available in workers, so audio chunks mix on MAIN and cross as transferred PCM (`Float32Array` channel planes, zero-copy transfer); the worker wraps them in mediabunny `AudioSample` (constructor accepts raw `AudioSampleInit` — no `AudioBuffer` needed) and feeds an `AudioSampleSource` instead of `AudioBufferSource`. `exportComposition`'s `mixAudioChunk` contract is already async per chunk — the cross-thread await drops into the existing seam unchanged. Audio mixing is a tiny share of export cost, and `OfflineAudioContext.startRendering` renders off-main natively, so main-thread jank from it is negligible.

*Architecture:* worker owns the orchestrator (`exportComposition` frame walk), compositor + Pixi backend on OffscreenCanvas, video pipelines, sink/muxer. Main owns: doc snapshot (`project.toJSON()` → postMessage — commands/functions never cross), asset handles (http(s)/blob URLs pass as strings; `File`/`Blob` structured-clone), audio chunk mixing, and the UI. Protocol (typed messages): `start{doc, options, assets}` → `audio-chunk?{fromUs,toUs}` / `audio-chunk{planes[],sampleRate,frames}` (transferred) → `progress{phase,framesDone,totalFrames,audioMixedUs,bytesWritten}` → `done{bytes?}` / `error{message}`; `abort` main→worker maps to the existing AbortSignal. Output: streaming `target` TRANSFERS the WritableStream into the worker (WritableStream is transferable — a `showSaveFilePicker()` writable streams straight from the worker); buffered mode transfers the final bytes once. Constraint carried from server-export: custom clip-kind `factories` are functions and cannot cross the boundary — worker export supports BUILT-IN kinds only; callers needing custom kinds keep main-thread `exportProject` (which stays, unchanged, as the compatibility path).

*Packaging (open decision):* (A) convenience-first — `exportProjectInWorker(project, options)` in `@miraiclip/renderer`, spawning `new Worker(new URL("./export.worker.js", import.meta.url), {type:"module"})` (the pattern Vite/webpack/Rollup all resolve; tsup gains a worker entry; a `workerUrl` option overrides for non-bundler setups); or (B) app-owned — the library exports the worker-side `runExportWorker()` entry + main-side `exportViaWorker(worker, doc, options)`, and the app instantiates the Worker (zero bundler coupling in the library, one extra step per consumer). Leaning A-on-top-of-B: ship both layers, A implemented via B.

*Verification:* e2e — worker export byte-compared against main-thread export of the same doc (determinism argument as streamed-vs-buffered); corpus gains a worker-export cell; long-export stress variant through the worker (heap flat on BOTH threads); repro:4k through the worker shows main-thread responsiveness (rAF cadence sampled on main while exporting). Playground adopts it behind the Export button (with `showSaveFilePicker` streaming when available) — which also retires the playground's buffered-whole-file cost.


**Gate-3 spec — the export validation corpus (drafted 2026-09-13).** Purpose: make "correct output" machine-checkable over a representative matrix, so the release gate is "100% corpus pass" and, later, the ≥99.9% success claim has a definition of success to count against.

*Independence principle.* Exports are produced by the real pipeline (headless Chromium, same as users); verification runs OUTSIDE Chromium — **ffprobe/ffmpeg in Node** parse containers, dump timestamps, and decode frames to raw pixels. Today's e2e verifies with a second decode *inside the same browser*; a corpus that catches muxer/codec bugs must not share the decoder under test. (The existing in-browser checks stay — they run per-commit; the corpus is the release/nightly tier.)

*Fixtures (generated by script, like the 4K fixture; committed only if tiny).* (a) `e2e-frames.webm` — existing frame-index fixture (frame N = color(N)); the pixel oracle. (b) **NEW sync fixture** — frame-index video PLUS a 20ms tone burst starting exactly at each 1-second boundary (silence elsewhere): decoded burst sample positions vs frame timestamps measure A/V offset at any point in the file, so drift = offset(end) − offset(start). (c) `e2e-key.webm` green-screen (chroma output check). (d) A color-bars still (PNG asset) — catches range/matrix errors (bt601 vs 709, full vs limited) that the frame-index tolerance can absorb. (e) The long-source fixture (60–120 min, local-only) joins the matrix when gate 4's work lands.

*The matrix.* Projects (each 4–12s, deliberately small — the corpus is wide, not long): single clip; a 6-cut chain (trim offsets prime relative to GOP size, so cuts land mid-GOP); all 5 transition kinds bridging known frame pairs; keyframed opacity + transform ramps; text + karaoke captions over video; 3-layer PIP; audio: tone at unity, volume-keyframe fade, transition crossfade, silent-video (must produce NO audio track), audio starting mid-timeline (leading silence must be real samples); a range export; a non-integer-fps-ratio case (30fps source → 24fps output — frame selection under resampling). Settings axes: webm draft + standard always; mp4 when the runner's Chrome has H.264 (real Chrome locally, skipped in CI Chromium with a visible skip); output fps 24/30; one non-source resolution. Not the full cross-product — each project pins the settings that stress it; target ≈ 25–35 exports, ~20–30 min under SwiftShader.

*Checks per output (all hard-fail).* 1) **Container**: ffprobe parses; stream count/dimensions/duration exact (duration within half an output frame). 2) **Timestamp integrity**: every frame's PTS from ffprobe — count == expected, strictly increasing, tiling k/fps exactly (container timebase rounding only), zero gaps/dups; audio PTS contiguous. 3) **Frame selection**: ffmpeg-decoded center pixel of EVERY frame maps to the expected source frame under the midpoint sampling rule — through cuts (the mid-GOP trims are the trap) and transitions (expected = blend math at that frame's p, per-kind). 4) **Color**: fixture colors round-trip within codec tolerance AND the color-bars project asserts bar values within a tight window (range/matrix errors shift all bars together — checked explicitly). 5) **A/V sync**: burst-position offset ≤ 1 output frame at the first AND last burst; drift between them ≤ 1 frame (the nightly long-output variant runs one 10-min export for real drift accumulation). 6) **Audio**: RMS unity on the tone case; fade case's RMS decay matches the keyframe curve; silent-video case has NO audio stream; mid-start case's leading samples are zeros of the right length. 7) **Streamed == buffered**: one matrix cell exports both ways; the streamed reassembly must byte-match the buffered file (same input, same encoder — any diff is a target bug).

*Harness.* `apps/playground/corpus/` — a Node runner (vitest, like server-export's stress tier) that drives exports through `@miraiclip/server-export` (it IS the browser pipeline, and gives the corpus mp4 coverage on real Chrome for free), then verifies with ffmpeg/ffprobe via child_process; results append per-cell to `corpus-results.json` (cell id, pass/fail, measured numbers, commit) — the artifact that later feeds the reliability ledger (a pass = one independent successful job toward the ~3,000 needed for the 99.9% bound; the matrix × nightly runs accumulates there honestly since cells differ). Root script `pnpm corpus`; nightly job after the stress job (needs ffmpeg on the runner — apt install, same as the 4K step); release checklist runs it locally on real Chrome for the mp4 half.

**Wave 1 ✅ BUILT + ALL GREEN 2026-09-13** (packages/server-export/corpus/, `pnpm corpus`, nightly job added to stress.yaml): 12 cells — single-clip, 6 mid-GOP cuts, dissolve blend math, range, 30→24fps resample, opacity ramp, audio unity/mid-start/fade, sync (measured A/V offset 0ms at first AND last burst), streamed==buffered BYTE-IDENTICAL — plus verifier self-tests against the sync fixture's ground truth and pure expectation-math self-tests (21 tests, ~40s under SwiftShader). Verifier lesson: measure audio on CHANNEL 0 (`pan=mono|c0=c0`), never ffmpeg's `-ac 1` downmix — constant-power stereo→mono downmix inflates correlated content's RMS by exactly √2 (caught by the corpus's own first run). yuv420 forbids 1×1 crops — center pixel reads decode a 2×2 block and average. Wave 2 pending: wipe/slide/dip cells, captions/text, PIP, color-bars range/matrix, mp4-on-real-Chrome, long sources.

*Non-goals (v1 of the corpus).* No VMAF/SSIM (reference renders + exact fixtures beat a universal quality score across content types — revisit if effects ever get lossy approximations); no 4K cells (the throughput benchmark covers scale; correctness is resolution-independent by construction until proven otherwise); no browser-matrix (Chromium-family only until Safari/Firefox export support is claimed anywhere).

**Templates (RVE-style) — yes, and cheaper than it looks.** A template in our model is a parameterized document generator: `(params) → commands/ProjectDocument` — pure functions over the core, no new runtime. Proposed `@miraiclip/templates`: typed template definitions (params schema via Zod, same pattern as the command catalog), a starter set matching the RVE categories that map onto v4 primitives (text effects ← keyframes+text clips; transitions ← v4 transitions; logo reveals/intros ← keyframes+effects; charts need a programmatic clip kind — see below). Sequence AFTER v4, since templates compose v4's primitives.

**Programmable editor (Remotion-style) — we are one, with one gap.** The core has been code-first since v1: command-driven JSON documents, undo/redo, an AI command catalog exported as JSON Schema for LLM tool-use, and server rendering from a doc. The paradigm gap vs Remotion: they render arbitrary React per frame; our document is declarative. The bridge is a **programmatic clip kind** (v5 candidate): a registered clip kind whose renderer calls user code `(canvas/container, clipTimeUs, params) → void` through the v4 clip-kind/effects registry — deterministic per-frame code clips (charts, generative graphics) with export parity preserved. React-per-frame interop stays a possible `@miraiclip/renderer-remotion` adapter (noted earlier); not a foundation (license).

**RVE use-case coverage map.** (1) Embed an editor in your app → engine is ready (core+renderer+export); the missing layer is UI: `@miraiclip/react` + an example editor app (already the parallel track). (2) AI video editing → strongest fit: the command catalog was DESIGNED for LLM tool-use; an agent emits commands, the user previews/undoes. (3) AI ad maker → templates + server export (shipped) + AI commands. (4) Social-app editing → v4 delivers the table stakes (karaoke captions, transitions, effects). (5) Internal tools → `miraiclip-export` CLI + JSON docs already fit. (6) Branded videos on demand → templates + a future "locked regions" concept (doc-level constraints on which properties commands may touch — natural fit for command validation; v5 candidate).

Sequencing implication: v4 (creative primitives) → v4.x (stress suite, `registerEffect` public, chunked audio) → v5 candidates (templates, programmatic clip kind, example editor app, locked regions).

### Adapters & ecosystem (parallel track)
- `@miraiclip/react` (hooks/selectors), later Vue and Svelte adapters
- AI helpers: command catalog export for LLM tool-use, prompt-to-commands examples
- Collaboration reference: patch/command sync example (e.g. over WebSocket; CRDT exploration later)
- Docs site with core-concepts pages mirroring this plan; example editor app

## Repository structure (pnpm monorepo)

```
miraiclip/
├── packages/
│   ├── core/          # @miraiclip/core — v1 focus
│   ├── renderer/      # @miraiclip/renderer (v2)
│   ├── react/         # @miraiclip/react
│   └── ...            # vue, svelte, export utils
├── apps/
│   ├── docs/          # documentation site
│   └── playground/    # example editor
├── package.json       # pnpm workspace
└── PLAN.md
```

Tooling: TypeScript, pnpm workspaces, Vitest, tsup (or Vite lib mode) for builds, Changesets for versioning/publishing, ESLint + Prettier, CI on GitHub Actions.

## v1 build order

1. Scaffold monorepo + `@miraiclip/core` package with types for ProjectState, Track, Clip, Asset
2. Zustand vanilla store + JSON (de)serialization
3. Command dispatcher with validation and typed errors
4. Track and clip commands with unit tests (the bulk of v1)
5. Patch emission via Immer patches; event emitter
6. History (undo/redo) from inverse patches; transactions
7. Timeline utilities; custom command API
8. Docs for core concepts + command catalog reference; publish 0.1.0

## Open questions (decide during v1)

- Command schema library: Zod (great DX) vs plain JSON Schema (portable to LLM tool definitions) — or Zod with JSON Schema export
- Patch format: Immer patches vs RFC-6902 JSON Patch (RFC-6902 is more interoperable for collaboration)
- ID strategy: nanoid vs ULID (ULIDs sort by creation time, handy for sync)
- Whether playback *position* lives in core state (undoable? probably excluded from history) — OpenVideo keeps it in the store but outside history
